import moment from "moment"
import {Container} from "semantic-ui-react"
const Book = ({days}) => {
  return (
    <Container textAlign="center">
      <h1>Book</h1>
    </Container>
  )
}

export default Book
